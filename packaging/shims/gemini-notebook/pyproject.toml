# GENERATED FILE — DO NOT EDIT.
# Regenerate with: python packaging/shims/generate.py
#
# ADR-0028 forwarding shim. Ships no Python modules and no console scripts;
# it exists so that `pip install gemini-notebook` (and every extra of it) resolves to
# the canonical distribution. See packaging/shims/README.md.

[project]
name = "gemini-notebook"
version = "0.9.0a1"
description = "Alias of gemini-notebook-py. Installing this package installs gemini-notebook-py; `import notebooklm` is unchanged and permanent."
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "Teng Lin", email = "teng.lin@gmail.com"}]
readme = "README.md"
dependencies = [
    "gemini-notebook-py==0.9.0a1",
]

# Every canonical extra is mirrored so that, e.g.,
# `pip install "gemini-notebook[mcp]"` installs exactly what
# `pip install "gemini-notebook-py[mcp]"` installs.
[project.optional-dependencies]
browser = ["gemini-notebook-py[browser]==0.9.0a1"]
cookies = ["gemini-notebook-py[cookies]==0.9.0a1"]
dev = ["gemini-notebook-py[dev]==0.9.0a1"]
headless = ["gemini-notebook-py[headless]==0.9.0a1"]
impersonate = ["gemini-notebook-py[impersonate]==0.9.0a1"]
markdown = ["gemini-notebook-py[markdown]==0.9.0a1"]
mcp = ["gemini-notebook-py[mcp]==0.9.0a1"]
server = ["gemini-notebook-py[server]==0.9.0a1"]
all = ["gemini-notebook-py[browser,dev,headless,markdown,mcp,server]==0.9.0a1"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

# No Python package to ship — force an empty wheel payload.
[tool.hatch.build.targets.wheel]
bypass-selection = true

[tool.hatch.build.targets.sdist]
include = ["README.md", "pyproject.toml"]
